#- name: variable for inventory
#  hosts: TEST
#  tasks:
#    - name: Print URL
#      ansible.builtin.debug:
#        msg: "URL - {{ URL }}"

#- name: Variable Example
#  hosts: localhost
#  vars:
#    COURSE: DevOps
#    TRAINER: Imran
#
#  tasks:
#    - name: Print variable
#      ansible.builtin.debug:
#        msg: "COURSE= {{ COURSE }} - Trainer= {{ TRAINER }}"

### Command line variables(-e)###
#- name: Extra variable
#  hosts: localhost
#  tasks:
#    - name: Print variable
#      ansible.builtin.debug:
#        msg: "Name = {{ NAME }}"

 ## server:-  ansible-playbook demo.yml -e NAME=imran #


### list variable ##

#- name: list example
#  hosts: localhost
#  vars:
#    TOOLS:
#      - LINUX
#      - DOCKER
#      - KUBERNETS
#  tasks:
#    - name: print list
#      ansible.builtin.debug:
#        msg: "{{ TOOLS }}"


## Dictionary variable##

#- name: Dictionary variable
#  hosts: localhost
#  vars:
#    Employee:
#      name: Imran
#      role: devops
#
#  tasks:
#    - name: Print directory
#      ansible.builtin.debug:
#        msg: "Name = {{ Employee.name }}, Role = {{ Employee.role }}"


## Register variable##

#- name: Register Example
#  hosts: localhost
#  tasks:
#    - name: Run command
#      ansible.builtin.shell: uptime
#      register: OUTPUT
#
#    - name: Print output
#      ansible.builtin.debug:
#        msg: "{{ OUTPUT.stdout }}"



- name: prompt your name
  hosts: localhost

  vars_prompt:
    - name: USERNAME
      prompt: Enter your name
      private: no
  tasks:
    - name: print
      ansible.builtin.debug:
        msg: "Hello {{ USERNAME }}"









